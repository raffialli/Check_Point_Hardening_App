{
  "name": "check-point-hardening-app-single-use-public-edition",
  "version": "2.0.0",
  "private": true,
  "description": "Open Public Edition hardening scanner for Check Point environments.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check public/app-release.js && node --check public/workbench.js && node --check public/finding-model.js && node --check lib/collection.js && node --check lib/operations.js && node --check lib/command-run.js && node --check lib/localhost-gate.js && node --check lib/version-info.js && node --check lib/git-repo.js && node --check lib/update-check.js && node --check lib/apply-update.js && node --check scripts/generate_report_pdf.mjs",
    "test": "node --test tests/*.test.mjs",
    "install:browsers": "playwright install chromium"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^4.10.38",
    "playwright": "^1.54.1"
  }
}
